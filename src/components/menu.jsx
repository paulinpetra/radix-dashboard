import { Button } from "@radix-ui/themes";

const Menu = () => {
  return (
    <div>
      <Button variant="soft">Mail</Button>
      <Button variant="soft">Dashboard</Button>
      <Button variant="soft">Cards</Button>
      <Button variant="soft">Tasks</Button>
      <Button variant="soft">Playground</Button>
      <Button variant="soft">Forms</Button>
      <Button variant="soft">Music</Button>
      <Button variant="soft">Authentication</Button>
    </div>
  );
};

export default Menu;
